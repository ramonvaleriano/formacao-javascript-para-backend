/**
 * Templete String.
 */

// Concatenando meu nome.
const primeiroNome = 'Ramon'
const espaco = ' '
const nomeDoMeio = 'Rodrigues'
const ultimoNome = 'Valeriano'

const nomeCompleto = primeiroNome + espaco + nomeDoMeio + espaco + ultimoNome

console.log(`Meu nome completo: ${nomeCompleto}`)