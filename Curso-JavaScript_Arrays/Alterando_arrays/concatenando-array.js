// Como juntar, concatenar duas lista, duas arrays.

const salaJS = ["Evaldo", "Camis", "Mari"];
const salaPython = ["Juliana", "Leoanardo", "Raquel"];

console.log("As duas salas inciais: ");
console.log(salaJS);
console.log(salaPython);

// Concatenando as duas salas.
// Adicioanndo os dados da salaPython e adicionando na salaJS.

const salaUnidas = salaJS.concat(salaPython);

console.log(`União das duas salas: `)
console.log(salaUnidas)