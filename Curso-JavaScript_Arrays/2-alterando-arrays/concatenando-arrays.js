/**
 * Alterando Arrays.
 * Concatenando Arrays.
 */

// Dados para serem trabalhados.

const salaJS = ["Evaldo", "Camis", "Mari"];
const salaPython = ["Ju", "Leo", "Raquel"];

// Concatenando os dados.

const salaTotal = salaJS.concat(salaPython);

console.log(`A sala JS: ${salaJS}`)
console.log(`A sala Python: ${salaPython}`)

console.log(`A sala total: ${salaTotal}`)